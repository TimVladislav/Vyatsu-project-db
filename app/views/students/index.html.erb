<nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="/start_page">DataBASE</a>
            </div>
            <div id="navbar" class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    <li><%= link_to 'Курсовые работы', articles_path %></li>
                    <li class="active"><%= link_to 'Студенты', students_path %></li>
                    <li><%= link_to 'Преподаватели', teachers_path %></li>
                    <li><%= link_to 'Статистика', statistics_path %></li>
                </ul>
            </div>
            <!--/.nav-collapse -->
        </div>
</nav>
<h1>Студенты</h1>
<br>
<table id="studentTable" class="table table-striped table-bordered tablesorter responsive">
  <thead>
  <tr>
    <th>ФИО</th>
    <th>Группа</th>
    <th>Дата сдачи</th>
    <th>Предмет</th>
    <th>Преподаватель</th>
    <th>e-mail</th>
    <th>Телефон</th>
    <th>Соц. сеть</th>
    <th><%= link_to 'Add', new_student_path %></th>
  </tr>
  </thead>
  <tbody>
  <% @students.each do |student|%>
    <tr>
      <td><%= student.fio %></td>
      <td><%= student.group %></td>
      <td><%= student.date_work %></td>
      <td><%= student.subject %></td>
      <td><%= student.teacher %></td>
      <td><%= student.email %></td>
      <td><%= student.tnumber %></td>
      <td><%= student.sn %></td>
      <td>
        <%= link_to 'Show', student_path(student) %>
        <%= link_to 'Edit', edit_student_path(student) %>
        <%= link_to 'Delete', student_path(student), method: :delete, data: {confirm: 'Вы уверены, что хотите удалить строку?' } %>
      </td>
    </tr>
  <% end %>
  </tbody>

</table>
<script type="text/javascript" charset="utf-8">
  $(document).ready(function(){
    //$("#articleTable").tablesorter({sortList: [[0,0],[1,0]]});
    $("#studentTable").dataTable();
  });
</script>
